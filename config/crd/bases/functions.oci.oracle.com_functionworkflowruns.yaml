---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: functionworkflowruns.functions.oci.oracle.com
spec:
  group: functions.oci.oracle.com
  names:
    categories:
    - oci
    - functions
    kind: FunctionWorkflowRun
    listKind: FunctionWorkflowRunList
    plural: functionworkflowruns
    singular: functionworkflowrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.nodeCount
      name: Nodes
      type: integer
    - jsonPath: .status.completedNodes
      name: Complete
      type: integer
    - jsonPath: .status.failedNodes
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FunctionWorkflowRun is the Schema for one execution of a FunctionWorkflow.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FunctionWorkflowRunSpec defines one execution of a FunctionWorkflow.
            properties:
              workflowRef:
                description: WorkflowRef references a FunctionWorkflow in the same
                  namespace.
                properties:
                  name:
                    description: Name is the referenced FunctionWorkflow name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - workflowRef
            type: object
          status:
            description: FunctionWorkflowRunStatus defines the observed state of FunctionWorkflowRun.
            properties:
              completedNodes:
                description: CompletedNodes is the number of nodes that completed
                  successfully.
                format: int32
                type: integer
              completionTime:
                description: CompletionTime is when the workflow run reached a terminal
                  phase.
                format: date-time
                type: string
              conditions:
                description: Conditions contain detailed workflow run state transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failedNodes:
                description: FailedNodes is the number of nodes that failed.
                format: int32
                type: integer
              nodeCount:
                description: NodeCount is the total number of nodes observed from
                  the workflow template.
                format: int32
                type: integer
              nodes:
                description: Nodes records per-node execution state.
                items:
                  description: FunctionWorkflowRunNodeStatus records execution state
                    for one workflow node.
                  properties:
                    completionTime:
                      description: CompletionTime is when the node reached a terminal
                        phase.
                      format: date-time
                      type: string
                    functionJobRef:
                      description: FunctionJobRef references the child FunctionJob
                        created for this node.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    message:
                      description: Message is a short human-readable node status summary.
                      type: string
                    name:
                      description: Name is the workflow node name.
                      type: string
                    phase:
                      description: Phase is the observed node execution phase.
                      enum:
                      - Pending
                      - Running
                      - Complete
                      - Failed
                      - Skipped
                      type: string
                    startedAt:
                      description: StartedAt is when the node child FunctionJob was
                        first observed or created.
                      format: date-time
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last metadata.generation observed
                  by the controller.
                format: int64
                type: integer
              observedWorkflowGeneration:
                description: ObservedWorkflowGeneration is the referenced workflow
                  generation used for this status.
                format: int64
                type: integer
              phase:
                description: Phase is a compact state summary for kubectl output.
                enum:
                - Pending
                - Running
                - Complete
                - Failed
                type: string
              skippedNodes:
                description: SkippedNodes is the number of nodes skipped because dependencies
                  did not complete successfully.
                format: int32
                type: integer
              startedAt:
                description: StartedAt is when the controller first observed the workflow
                  run.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
