---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: functions.functions.oci.oracle.com
spec:
  group: functions.oci.oracle.com
  names:
    categories:
    - oci
    - functions
    kind: Function
    listKind: FunctionList
    plural: functions
    singular: function
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.functionOcid
      name: Function OCID
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Function is the Schema for the functions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FunctionSpec defines the desired state of Function.
            properties:
              config:
                description: Config describes desired function configuration for future
                  lifecycle management.
                properties:
                  applicationOcid:
                    description: ApplicationOCID is the OCI Functions application
                      OCID that owns the function.
                    pattern: ^ocid1\.fnapp\..+
                    type: string
                  displayName:
                    description: DisplayName is the OCI function display name.
                    maxLength: 255
                    minLength: 1
                    type: string
                  environment:
                    additionalProperties:
                      type: string
                    description: Environment is a future OCI function config map.
                    type: object
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: FreeformTags are OCI freeform tags to apply when
                      lifecycle management is implemented.
                    type: object
                  image:
                    description: Image is the container image reference for the function.
                    minLength: 1
                    type: string
                  memoryInMB:
                    default: 128
                    description: MemoryInMB is the memory size to configure when lifecycle
                      management is implemented.
                    enum:
                    - 128
                    - 256
                    - 512
                    - 1024
                    - 2048
                    format: int32
                    type: integer
                  timeoutInSeconds:
                    default: 30
                    description: TimeoutInSeconds is the max execution time for the
                      function.
                    format: int32
                    maximum: 300
                    minimum: 1
                    type: integer
                required:
                - applicationOcid
                - displayName
                - image
                type: object
              existingFunctionOcid:
                description: |-
                  ExistingFunctionOCID points at an existing OCI Functions function.
                  Deprecated: use FunctionID.
                pattern: ^ocid1\.fnfunc\..+
                type: string
              functionId:
                description: FunctionID points at an existing OCI Functions function
                  OCID.
                pattern: ^ocid1\.fnfunc\..+
                type: string
            type: object
            x-kubernetes-validations:
            - message: one of spec.functionId, spec.existingFunctionOcid, or spec.config
                is required
              rule: has(self.functionId) || has(self.existingFunctionOcid) || has(self.config)
            - message: spec.config is mutually exclusive with existing function references
              rule: '!(has(self.config) && (has(self.functionId) || has(self.existingFunctionOcid)))'
            - message: spec.functionId and spec.existingFunctionOcid are mutually
                exclusive
              rule: '!(has(self.functionId) && has(self.existingFunctionOcid))'
          status:
            description: FunctionStatus defines the observed state of Function.
            properties:
              conditions:
                description: Conditions contain detailed state transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              functionOcid:
                description: FunctionOCID is the resolved OCI function OCID when known.
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the controller updated
                  observed status.
                format: date-time
                type: string
              message:
                description: Message is a short human-readable status summary.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last metadata.generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is a compact state summary for kubectl output.
                enum:
                - Pending
                - Ready
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
